services:
  postgres:
    image: postgres:15
    environment:
      POSTGRES_DB: inventory_db
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    ports:
      - "5432:5432"
    volumes:
      - pg_data_dev:/var/lib/postgresql/data
      - ./java-web-spring-api-autoflex/database/backups:/backups
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres -d inventory_db"]
      interval: 5s
      timeout: 5s
      retries: 20

  restore:
    image: postgres:15
    depends_on:
      postgres:
        condition: service_healthy
    restart: "no"
    environment:
      PGPASSWORD: postgres
    volumes:
      - ./java-web-spring-api-autoflex/database/backups:/backups
    command: >
      sh -c "
      if [ -f /backups/inventory_db.backup ]; then
        echo 'Restaurando backup /backups/inventory_db.backup...';
        pg_restore -h postgres -U postgres -d inventory_db --clean --if-exists --no-owner --no-privileges /backups/inventory_db.backup;
        echo 'Restore finalizado.';
      else
        echo 'Backup nao encontrado em /backups/inventory_db.backup. Restore ignorado.';
      fi
      "

  backup:
    image: postgres:15
    depends_on:
      postgres:
        condition: service_healthy
    restart: always
    environment:
      PGPASSWORD: postgres
    volumes:
      - ./java-web-spring-api-autoflex/database/backups:/backups
    command: >
      sh -c "
      while true; do
        echo 'Gerando backup...';
        pg_dump -h postgres -U postgres -d inventory_db -Fc -f /backups/inventory_db.backup.tmp &&
        mv /backups/inventory_db.backup.tmp /backups/inventory_db.backup &&
        echo 'Backup atualizado em /backups/inventory_db.backup';
        sleep 60;
      done
      "

  backend:
    image: maven:3.9.9-eclipse-temurin-11
    working_dir: /workspace/java-web-spring-api-autoflex
    depends_on:
      postgres:
        condition: service_healthy
    environment:
      SPRING_PROFILES_ACTIVE: prod
      SPRING_DATASOURCE_URL: jdbc:postgresql://postgres:5432/inventory_db
      SPRING_DATASOURCE_USERNAME: postgres
      SPRING_DATASOURCE_PASSWORD: postgres
    volumes:
      - ./java-web-spring-api-autoflex:/workspace/java-web-spring-api-autoflex
      - maven_cache:/root/.m2
    ports:
      - "8080:8080"
    command: sh -c "mvn spring-boot:run"

  frontend:
    image: node:20-alpine
    working_dir: /workspace/web-app-autoflex
    depends_on:
      - backend
    environment:
      CHOKIDAR_USEPOLLING: "true"
    volumes:
      - ./web-app-autoflex:/workspace/web-app-autoflex
      - frontend_node_modules:/workspace/web-app-autoflex/node_modules
    ports:
      - "5173:5173"
    command: sh -c "npm install && npm run dev -- --host 0.0.0.0 --port 5173"

volumes:
  pg_data_dev:
  maven_cache:
  frontend_node_modules:
